# 强效跟随 — 功能介绍

> 基于代码实际行为整理（对应 `AutoFollow.csproj` 版本 **1.8.0**）

---

## 目录

1. [核心逻辑](#核心逻辑)
2. [状态机](#状态机)
3. [主循环 — 每帧行为](#主循环--每帧行为)
4. [目标解析](#目标解析)
5. [战斗区进出](#战斗区进出)
6. [循环插件协同](#循环插件协同)
7. [疾跑控制](#疾跑控制)
8. [坐骑控制](#坐骑控制)
9. [两段式惯移（目标丢失/远距跃迁）](#两段式惯移目标丢失远距跃迁)
10. [Boss 战保护](#boss-战保护)
11. [地图黑名单](#地图黑名单)
12. [换图自动恢复跟随](#换图自动恢复跟随)
13. [自动接受传送邀请](#自动接受传送邀请)
14. [副本通关后暂停](#副本通关后暂停)
15. [迷你窗口](#迷你窗口)
16. [设置窗口](#设置窗口)
17. [命令列表](#命令列表)
18. [配置默认值](#配置默认值)
19. [依赖与常见问题](#依赖与常见问题)

---

## 核心逻辑

```
Plugin (入口)
├── Configuration → FollowConfig (各项参数)
├── Log (全局日志/聊天输出，静态类，各模块直接调用)
├── CustomCommandManager (/ff /ftar /fst 等命令)
├── FollowEngine (核心跟随引擎)
│   ├── IPCService → LoopController (暂停/恢复自动输出插件)
│   ├── VnavmeshFollow → VnavmeshIPC (寻路)
│   ├── ConditionManager (战斗/坐骑/死亡状态检测)
│   └── SprintController (疾跑/坐骑控制)
├── MiniWindow (迷你窗口：三态按钮 + 旗标折叠区)
└── DebugWindow (调试/设置窗口)
```

**工作流**：

```
设定目标 → 每帧 GetObjectTable 找目标
  ├─ 找不到 → 两段式惯移（先最后已知点 → 再面朝惯移 1.5s / 2s）
  ├─ 找到但 ≤5y → 停移动 + 恢复循环插件 + 进 Combat 态
  ├─ 找到但 ≤10y → 停跟随 + 恢复循环插件 + 进 Combat 态
  └─ 找到且 >10y → 疾跑/坐骑判断 → vnavmesh 寻路追赶
```

---

## 状态机

| 状态 | 含义 | 切换条件 |
|------|------|----------|
| `Idle` | 未启动 | 无目标 |
| `Following` | 正常跟随中 | 有目标且距离 > 进入战斗区 |
| `CatchingUp` | 追赶中 | 预留（当前未使用，与 Following 合并处理） |
| `Combat` | 进入战斗范围 | 距离 ≤ 进入战斗区 或 ≤5y |
| `Paused` | 手动/条件暂停 | 用户 `/ff` 或 `Pause()` |
| `TargetLost` | 目标丢失 | 惯移结束仍无目标 |
| `EmergencyStopped` | 紧急停止 | 热键 / 迷你窗暂停按钮 |

**PauseReason**：每个暂停状态附带友好描述，用于迷你窗口和 UI 显示：
- `Combat` → "战斗中"
- `Paused` → "手动暂停" / "地图黑名单" / "副本已完成"
- `EmergencyStopped` → "紧急停止"
- `TargetLost` → "目标丢失"

---

## 主循环 — 每帧行为

`FollowEngine.OnTick` 每帧执行（`Following` / `Combat` 态时注册 `IFramework.Update`）：

### 顺序

1. **惯移处理**：若 `_inertiaPhase != None` → 走 `HandleInertia()`，跳过后续
2. **空闲态拦截**：`Idle` / `Paused` / `EmergencyStopped` → 直接返回
3. **≤5y 停走**：每帧（独立于扫描间隔）检测距离 ≤5y → 停移动 + 恢复循环 + 进 Combat
4. **地图黑名单**：`CheckBlacklistedMap()` → 当前地图在黑名单中则暂停
5. **脱战检测**：`ConditionManager.Update()` 每帧更新，脱战 1s 后自动恢复跟随（距离近则跳过防抖）
6. **扫描间隔**：`ScanInterval`（默认 2s）到点才执行以下操作
7. **目标解析**：`ResolveTarget()` 按 ID 优先 → 名称匹配
8. **目标丢失**：找不到目标 → 启动两段式惯移 或 直接清目标
9. **距离检测**：
   - 远距跃迁（≤100y → >100y）→ 启动两段式惯移
   - ≤ 进入战斗区（10y）→ 停跟随 + 恢复循环 + 进 Combat
   - > 离开战斗区（30y）且当前 Combat → 恢复跟随 + 暂停循环（Boss 战不恢复）
10. **疾跑/坐骑**：非 Combat 态才执行
11. **≤5y 停走**（再次检查，防近距离抖动）
12. **移动阈值**：目标位移 < `MoveThreshold`（5y）不重发路径
13. **发路径**：`vnavmesh.MoveTo(from, to)` 异步寻路

### 硬编码常量

| 常量 | 值 | 说明 |
|------|-----|------|
| 停止距离 | 5y | 每帧检测，独立于扫描间隔 |
| 脱战延迟 | 1s | 脱战后等待 1s 再恢复跟随 |
| 启动保护 | 2s | 设置目标后 2s 内不触发暂停 |
| 惯移步1超时 | 30s | 前往最后已知点超过 30s 则提前进入面朝阶段 |
| 面朝惯移时长 | 1.5s | 面朝方向移动 1.5s |
| 面朝总窗口 | 2s | 面朝阶段总时间（1.5s 移动 + 0.5s 停等） |
| 惯移取消距离 | 100y | 目标重现在 ≤100y 时取消惯移 |
| 最后已知点到达 | 3y | 视为到达最后已知坐标的距离 |
| 面朝惯移距离 | 30y | 面朝方向移动距离 |
| 上马等待 | 5s | 上坐骑超时后回退步行 |
| 传送等待 | 20s | 跨区传送超时取消 |

---

## 目标解析

`ResolveTarget()` 按优先级查找：

1. **ObjectIndex 匹配**：如果 `_followTargetId` 不为 null，用 `SearchById` 直接查
2. **名称匹配**：遍历 `ObjectTable`，找 `ObjectKind.Pc` 且名称相符的第一个玩家
3. 找到后更新 `_followTargetId` 为 ObjectIndex（加速下次查找）

`SetTarget(name)` ：
- 清空 → `null` 或空字符串 → 清目标、停移动、进 Idle
- 设名 → 设名称、尝试 ResolveTarget、启动引擎、进 Following

---

## 战斗区进出

| 条件 | 行为 |
|------|------|
| 距离 ≤ 进入战斗区（默认 10y） | `vnavmesh.Stop()` → `IPC.ResumeLoop()` → 进 `Combat` 态 |
| 距离 > 离开战斗区（默认 30y）且当前 Combat 且非 Boss | `IPC.PauseLoop()` → 进 `Following` 态 |
| 距离 ≤5y（每帧） | 同上（独立于扫描间隔） |

**启动保护**：设目标后 2s 内不触发暂停（`_followStartTime`）。

**脱战恢复**：脱离战斗 1s 后若目标仍在 `CombatEnterRange` 外则自动恢复跟随。若距离仍 ≤ 进入战斗区则跳过恢复（防抖）。

---

## 循环插件协同

通过 `CommandLoopController` 发送陌语命令控制第三方循环插件。

| 配置 | 默认 | 说明 |
|------|------|------|
| 暂停命令 | `/rotation off` | 进入战斗区时发送 |
| 恢复命令 | `/rotation Auto` | 离开战斗区时发送 |

**无恢复命令时**：再次发送暂停命令（部分插件 toggle 行为）。

**状态机集成**：
- 目标走远 → `PauseLoop()` → 暂停循环输出
- 接近目标 → `ResumeLoop()` → 恢复循环输出
- 紧急停止 → `PauseLoop()` → 暂停循环

---

## 疾跑控制

`SprintController` 控制所有疾跑逻辑。

### 模式

| 模式 | 配置 | 行为 |
|------|------|------|
| 智能疾跑（默认） | `SprintEnabled=true`，其余 false | 目标带疾跑状态(status 50) 或距离 > `SprintThreshold`(20y) → 强制疾跑；否则按距离 `Update` |
| 无脑疾跑 | `SprintAlwaysOn=true` | 跟随中一直按疾跑（CD 好了就按） |
| 仅战斗疾跑 | `SprintOnlyInCombat=true` | 仅战斗中按疾跑 |
| 坐骑模式 | `UseMount=true` | 脱战→上坐骑，战斗中→疾跑 |

### 实现细节

- `TrySprint()`：每 1 秒尝试一次，防刷屏
- 走 `ActionManager.UseAction(ActionType.GeneralAction, 4)` 原生调用
- 检查 `IsActionOffCooldown` + `GetActionStatus` 确保可用
- `TryForceSprint()`：强制尝试（无视距离检查），跟随时调用
- `TryMount()`：`ActionType.Mount, mountId=1`（陆行鸟），每 5 秒尝试一次

---

## 坐骑控制

`SprintController.TryMount()` 在 `UseMount=true` 且脱战且非坐骑态时自动上马。

- 走 `ActionManager.UseAction(ActionType.Mount, 1)` 原生调用（陆行鸟）
- 战斗中自动切换为疾跑
- 每 5 秒尝试一次，防刷屏

---

## 两段式惯移（目标丢失/远距跃迁）

当目标从 `ObjectTable` 消失或距离从 ≤100y 突然跳到 >100y 时，执行两段式惯移：

### 阶段 1：前往最后已知点 (`ToLastKnown`)

- 发 `vnavmesh.MoveTo` 到 `_lastKnownTargetPos`
- 到达（≤3y）或 vnav 停且够近 → 进入阶段 2
- 超时（30s）→ 进入阶段 2
- 无最后已知点 → 直接阶段 2

### 阶段 2：面朝惯移 (`FaceForward`)

- 沿玩家面朝方向 30y 发路径
- 1.5s 内保持移动
- 1.5s~2s 停止移动，等待窗口结束
- 2s 到期 → 正常收尾

### 取消条件

- 手动暂停/紧急停止/空闲 → 直接清惯移
- 目标重新出现在 ≤100y → 取消惯移，恢复跟随

### 收尾

- 惯移结束仍有目标 → 恢复跟随
- 惯移结束仍无目标 → 停移动，按 `PauseOnTargetLost` 配置清目标或进 Idle

---

## Boss 战保护

`IsBossTarget()` 扫描 `ObjectTable` 中 `BattleNpc`：

| 等级 | 相对玩家 MaxHP |
|------|----------------|
| ≥80 | > 20 倍 |
| 50–79 | > 15 倍 |
| 1–49 | > 10 倍 |

**生效处**：
- Combat 且距离 > 离开战斗区：若检测到 Boss → **不**恢复跟随
- `ResumeFollow()` 内：若仍在战斗且有 Boss → 直接 return

---

## 地图黑名单

设置页「地图黑名单」：填 `TerritoryType` 数值 ID。

在黑名单地图中：停寻路，状态 `Paused`，原因「地图黑名单」；`CheckBlacklistedMap()` 每帧拦截。

---

## 换图自动恢复跟随

`CheckZoneChange()` 帧回调，`_lastTerritory` 跟踪上次区域：

- 区域不同 → 等 `BetweenAreas` 结束 → 延迟 1.5s → `FollowEngine.Start()`
- 只恢复曾经设过目标（`TargetName != null`）且不在 `Following` 态的情况

配置：`AutoFollowAfterZoneChange`（默认开）。

---

## 自动接受传送邀请

`CheckTeleportInvitation()` 帧回调，检测 `SelectYesNo` addon 自动点"是"。

### 守卫条件

- `AutoAcceptTeleport` 开关
- 引擎状态为 `Following` / `CatchingUp` / `Combat` / `TargetLost`（跟随相关状态均接受）
- 500ms 节流防止重复点击

### 实现细节

1. `GetSelectYesnoAddon()` → 尝试 `SelectYesno`（小写 n）→ 失败则 `SelectYesNo`（大写 N）
2. `ResolveAddonPtr()` → 用 `IGameGui.GetAddonByName` + try/catch，返回 `AtkUnitBase*`
3. 文本读取优先级：`AddonSelectYesno.PromptText` → `AtkTextNode[2]` → `TryReadSelectYesnoText()`（扫描所有文本节点）
4. `IsTeleportPrompt()` → 匹配"传送"、"瞬移"、"Teleport"、"想要对你使用"
5. 匹配成功 → `FireCallbackInt(0)` 点"是"

---

## 副本通关后暂停

`IDutyState.DutyCompleted` 事件：

- 配置 `PauseOnDutyComplete`（默认开）
- 副本通关后自动 `Pause("副本已完成")`，状态变 `Paused`
- 防止出本后继续无意义跟随

---

## 迷你窗口

默认打开；无标题栏；可拖动。

### 指示圈

| 状态 | 形状 | 颜色 |
|------|------|------|
| 空闲 | 空心圆环 | 灰 |
| 跟随中 | 实心圆 + 白边 | 绿 |
| 暂停/战斗/丢失/急停 | 实心 + ⏸ 双杠 | 琥珀/红/橙/亮红 |

### 主按钮

| 当前按钮状态 | 点击行为 |
|-------------|----------|
| 跟随（Idle） | `TrySmartFollow`：选中玩家则跟该玩家；选中敌人则跟敌人的目标（常为坦克） |
| 暂停（Following） | `EmergencyStop`（急停，非普通 Pause） |
| 继续（Paused） | 优先 `TrySmartFollow`；失败则旧目标在 **≤30y** 时 `Resume`；更远则提示无法恢复 |

按钮颜色可自定义（设置页 → Mini 窗口按钮颜色）。

### 旗标操作折叠区

展开后显示玩家坐标、旗标 X/Z、三个按钮：

| 按钮 | 行为 |
|------|------|
| 步行至flag | `/vnav moveflag` |
| 飞行至flag | 自动上陆行鸟 → `/vnav flyflag`；跨区自动找水晶传送；上马失败 5s 回退步行 |
| 停止移动 | `/vnav stop` |

### 右键菜单

- 打开主窗口
- 状态报告
- 紧急停止
- 30y 内队友列表跟随
- 关闭窗口

---

## 设置窗口

`/fdbg` 打开，标签页：

| 页 | 内容 |
|----|------|
| 设置 | 距离阈值（可恢复默认）、其他控制（目标丢失续行/换图恢复/自动传送/副本暂停）、疾跑/坐骑、聊天提示、急停热键、循环命令、Mini 按钮颜色 |
| 坐标 | 玩家/目标/旗标与手动坐标相关操作 |
| 插件状态 | 扫描 vnavmesh / BossMod / RSR 等安装与连接 |
| 使用说明 | 游戏内帮助 |
| 地图黑名单 | Territory ID 列表 |
| 指令日志 | 默认启用；同内容 10 秒去重；分类过滤 / 复制 / 清空 |

---

## 命令列表

所有命令在 `FollowConfig.CustomCommands` 中定义，可在设置页自定义。

| 命令 | 实际作用 |
|------|----------|
| `/ftar` | 跟随当前选中目标名称，成功后清空选中 |
| `/ft <名>` | 按名字设目标；无参数只提示用法 |
| `/ff` | `Toggle` 跟随开/关 |
| `/fes` | 紧急停止 |
| `/fst` | 状态报告 |
| `/fdbg` | 开关设置窗口 |
| `/flp` | 暂停循环插件 |
| `/flr` | 恢复循环插件 |
| `/flt` | 仅发送暂停循环命令 |

默认急停热键：**F8**（可改）。

---

## 配置默认值

| 参数 | 默认 | 备注 |
|------|------|------|
| CombatEnterRange | 10 | 进入战斗区 |
| CombatExitRange | 30 | 离开战斗区 |
| ScanInterval | 2 | 秒 |
| MoveThreshold | 5 | y |
| SprintThreshold | 20 | y |
| SprintEnabled | true |  |
| SprintAlwaysOn | false |  |
| SprintOnlyInCombat | false |  |
| UseMount | false |  |
| ChatOutput | false |  |
| ContinueOnTargetLost | true | 其他控制项 |
| AutoFollowAfterZoneChange | true | 其他控制项 |
| AutoAcceptTeleport | true | 其他控制项 |
| PauseOnDutyComplete | true | 其他控制项 |
| PauseCommand | `/rotation off` |  |
| ResumeCommand | `/rotation Auto` |  |
| EmergencyStopKey | F8 |  |
| 按钮色 空闲/跟随/暂停 | 灰 / 绿 / 琥珀 | 可自定义 |

硬编码常量（无配置项）：停止距离 5y、脱战延迟 1s、启动保护 2s、丢失续行 1.5s、远距跃迁 100y / 惯移 1.5s / 总暂停 2s、最后已知点到达 3y、面朝惯移距离 30y、上马等待 5s、传送等待 20s、传送节流 500ms。

---

## 依赖与常见问题

| 插件 | 必需 | 说明 |
|------|------|------|
| vnavmesh | 是 | 寻路 / moveflag / flyflag |
| BossMod / BossModReborn | 否 | 停跟后由你自己的战斗 AI 接管 |
| RotationSolverReborn / AEAssist 等 | 否 | 靠暂停/恢复命令配合 |

**跟了不走？** vnavmesh 是否启用、NavMesh 是否加载；看 `/fdbg` → 指令日志是否有 move。

**循环不停/不恢复？** 核对暂停/恢复命令；默认 RSR；AEAssist 常用暂停 `/aestop`、恢复留空。

**怎么换目标？** `/ft 新名字` 或重新 `/ftar` / 迷你窗智能跟随。

**继续提示 >30y？** 迷你窗「继续」恢复旧目标时要求距离 ≤30y。

**迷你窗没了？** 卫月插件列表打开显示，或 `/fdbg`。

**飞行至flag 无效？** 需 vnavmesh；跨区需已开对应水晶；坐骑失败 5s 后回退步行。

**ProcessCommand 限制：** 只跑插件注册命令（如 `/vnav`）；`/mount`、`/tp` 无效，故上马与传送走 `UseAction` / `Telepo`。