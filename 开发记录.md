# 强效跟随 — 开发记录

> 开始开发前请先阅读本文，了解项目历史、关键决策和注意事项。

---

## 当前版本

- **v1.8.1** — 副本通关暂停加活跃状态过滤 + 扫描间隔默认 2s→1s
- **API 版本**: 卫月 API 15 (国服), .NET 10.0-windows
- **目标框架**: `net10.0-windows`, x64

---

## 成品位置

```
D:\gongju\FF14\开发\强效跟随\成品\
```

编译后自动复制到此文件夹，直接用卫月加载即可。

> 注意：仅 Release 配置的构建会自动复制到成品文件夹，Debug 构建不会。

---

## 版本历史

| 版本 | 变更 |
|------|------|
| v1.8.1 | 副本通关暂停仅在引擎活跃状态（跟随/追赶/战斗/目标丢失）时触发，空闲/已暂停/紧急停止直接忽略；扫描间隔默认 2s→1s（坐标/路径响应更快，≤5y 停走仍为每帧） |
| v1.8.0 | 全局 Log 重构：新增 `Utilities/Log.cs` 静态类，统一日志+聊天输出；移除各模块中 `IChatGui` / `IPluginLog` 依赖；聊天消息带 AF 图标 + XivChatType.Echo |
| v1.7.1 | 零风险死代码清理：删除未用 `StatusWindow` / `DistanceZone` / `FollowMode`；移除 Debug 窗未读的 `getBossActive`；删除无人调用的 `SmartFollow` 包装；`DebugLog.Enabled` 默认 true（与启动行为一致）；`FormatHelper.Dist` 去掉重复分支 |
| v1.7.0 | 自动接受队友传送邀请 (SelectYesNo 检测 + FireCallback 点"是")，目标远距暂停 2s 惯性移动 1.5s (_lastScanDistance 跃迁检测 + 面朝向移动)，目标丢失续行 1.5s，换图自动恢复跟随，其他控制分类 UI |
| v1.6.0 | 旗标飞行自动跨区传送 (Telepo 原生传送 + 区域检测 + IAetheryteList 找水晶 + 轮询等待)，上坐骑命令改为 UseAction(Mount,1) 陆行鸟，折叠按钮居中对齐 + 间距调整 |
| v1.5.6 | FlagMiniWindow 合并进 MiniWindow 折叠区，新增飞行至flag自动上坐骑+失败步行回退，修复 API 15 兼容性 (ObjectKind.Player → Pc, ImGui.NET 引用调整) |
| v1.5.5 | 跟随性能优化 + 参数调优 (PathfindVersion 防重复, 默认值 ScanInterval=2s, MoveThreshold=5y) |
| v1.5.4 | 暂停恢复时优先 TrySmartFollow 判断当前选中目标 |
| v1.5.3 | Mini 窗口按钮颜色可自定义 (BtnColorIdle/Following/Paused 配置项) |
| v1.5.2 | 暂停恢复逻辑优化 + 迷你窗口交互调整 (ButtonAction → TrySmartFollow + Resume) |
| v1.5.1 | 使用说明重写 + UI/UX 交互优化 (右键菜单颜色, 移除命令大全) |
| v1.5.0 | Mini 窗口三态按钮重构 (MiniMainButton 逻辑重写, ButtonAction 分离) |
| v1.4.12 | 坐骑模式战斗中改用疾跑，移除恢复强制疾跑 |
| v1.4.10 | 优化 TerritoryType 读取优先级，GameMain 原生读取优先 |
| v1.4.9 | Boss 检测扫描全场景 NPC，优化 TerritoryType 读取 |
| v1.4.5 | 初始提交 (Initial commit) |

---

## 核心架构

### 模块关系

```
Plugin (入口)
├── Configuration → FollowConfig (各项参数)
├── CustomCommandManager (/ff /ftar /fst 等命令)
├── FollowEngine (核心跟随引擎)
│   ├── IPCService → LoopController (暂停/恢复自动输出插件)
│   ├── VnavmeshFollow → VnavmeshIPC (寻路)
│   ├── ConditionManager (战斗/坐骑状态检测)
│   └── SprintController (疾跑控制)
├── MiniWindow (迷你窗口：三态按钮 + 旗标折叠区)  ← v1.5.6 合并旗标操作
└── DebugWindow (调试/设置窗口)
```

### 关键概念

- **三态按钮 (MiniMainButton)**: Idle → TrySmartFollow(优先新的选中目标) → Resume(恢复旧目标)
- **5y 停止距离**: 每帧检测（非扫描间隔内），距离 ≤5y 时停止跟随 + 恢复循环插件 + 切战斗状态
- **旗标操作**: 折叠在 MiniWindow 中，展开后显示旗标坐标(X Z)和三个描边按钮（步行/飞行/停止）。飞行至flag自动上坐骑+失败步行回退。跨区域自动传送。
- **远距目标暂停**: `_lastScanDistance` 跟踪上次扫描距离。目标从 ≤100y 突跳到 >100y 时暂停 2s，面朝方向惯性移动 1.5s，到期自动恢复
- **传送邀请自动接受**: 跟随态下检测 `SelectYesNo` addon 文字含"传送"，`FireCallback(1, &Int=0)` 自动点"是"

---

## 重要技术决策

### 1. 暂停恢复机制 (v1.5.2)

**方案**: 黄色按钮逻辑 = TrySmartFollow 优先（用户可能选了新目标）→ 失败则 Resume 旧目标

**原因**: 原方案直接 Resume 可能忽略用户手动切换目标的行为。

---

### 2. 按钮描边文字 (v1.5.1)

**方案**: ImGui Button 正常渲染 + DrawList 在按钮上方叠加 8 方向黑色描边文字 (alpha=0.85)

**原因**: 纯 DrawList 文字缺少 Button 的交互反馈（按下/悬停状态）。保留 Button 的交互行为，用 DrawList 描边增强可读性。

---

### 3. Mini 窗口关闭 X 移除 (v1.5.1)

**方案**: X 按钮 → 右键菜单关闭

**原因**: 窗口窄小时 X 按钮占用空间且影响布局。确认弹窗因 ImGui popup 系统限制无法正常工作，直接执行 `IsOpen = false`。

---

### 4. 5y 停止距离检测时机 (v1.5.5)

**方案**: 每帧检测（`OnTick`），在扫描间隔之前

**原因**: 若放在 2s 扫描间隔内检测，角色可能行走超出 5y 极限才发现。

**同时**: 5y 停止时调用 `_ipc.ResumeLoop()` + `SetState(Combat)`，否则循环插件不会恢复输出。

---

### 5. Pathfind 版本号防重复 (v1.5.5)

**方案**: VnavmeshIPC 中添加 `_pathfindVersion` 计数器，每次回调优先校验版本号

**原因**: 异步 PathFindMove 完成后回调可能携带过期数据（用户已发起新的寻路请求），导致路径错乱。

---

### 6. 旗标坐标读取 (v1.5.6)

**方案**: `AgentMap.Instance()->FlagMapMarkers[0]`，`XFloat`=世界X, `YFloat`=世界Z

**原因**: `IGameGui` 没有 `GetFlagMarkerPosition` 方法。`Map.Instance()` 结构体也缺少旗标字段。旗标无高度（Y）数据。

---

### 7. ImGui.NET 引用方式 (v1.5.6)

**运行时环境**: **API 15 没有原生 ImGui.NET.dll**（只有 `Dalamud.Bindings.ImGui.dll`），插件必须自带。

**方案** (当前): 
```xml
<Reference Include="ImGui.NET">
  <HintPath>$(AppData)\XIVLauncher\addon\Hooks\dev\ImGui.NET.dll</HintPath>
  <Private>true</Private>
</Reference>
```

**说明**: 
- 编译时需要 `dev\ImGui.NET.dll` 存在（从 `12.x` 版本文件夹复制 v1.82.0.0）
- 运行时 API 15 不提供 ImGui.NET 类型，插件必须自带
- **版本必须匹配**: 之前误用 NuGet 缓存中的 `v1.89.9.1`（`Begin` 用 `ReadOnlySpan<char>` 签名），运行时 API 15 版用的是 `v1.82.0.0`（`Begin` 用 `string` 签名），导致 `MissingMethodException`
- `dev\ImGui.NET.dll` 来源：`%AppData%\XIVLauncher\addon\Hooks\12.0.1.4-4-g0cbb4be2\ImGui.NET.dll`

**历史**: 旧 Debug 构建 (v12 API) 也是 `<Private>true</Private>` 自带 ImGui.NET。

### 8. ObjectKind.Player → ObjectKind.Pc (v1.5.6)

**原因**: Dalamud API 15 将枚举值重命名，`Player` 不再存在，应使用 `Pc`。

**位置**: Plugin.cs (2处), FollowEngine.cs (1处)

---

### 9. 旗标飞行跨区域自动传送 (v1.6.0)

**方案**: 在飞行至 flag 前通过 `FlagMapMarker.TerritoryId` 检测旗标是否在当前区域。
- 同区域 → 直接上坐骑 + `/vnav flyflag`
- 不同区域 → `Telepo.Teleport(aetheryteId, 0)` 原生传送 → 轮询等待 `BetweenAreas` 结束 → 确认目标区域 → 上坐骑 + `/vnav flyflag`

**关键依赖**:
- `FlagMapMarker.TerritoryId` — 旗标所在区域 ID (与 `_clientState.TerritoryType` 对比)
- `IAetheryteList` — Dalamud 服务，获取已开通水晶列表
- `Telepo.Teleport(uint, byte)` — FFXIVClientStructs 原生传送函数（比 `ProcessCommand("/tp")` 更可靠）

**不上坐骑状态保护**: 传送后 `StartMountAfterTeleport` 先检查 `ConditionFlag.Mounted`，已上坐骑则直接飞，否则上坐骑再飞。

**版本号防重复**: 整个流程（传送→到达→上坐骑→飞）共用 `_flyVersion` 版本号，防止多次点击产生冲突轮询链。

---

### 10. 上坐骑命令 (v1.6.0)

**方案**: `UseAction(ActionType.Mount, 1)` — mountId 1 = 陆行鸟

**原因**: 
- `ProcessCommand("/mount")` 不转发游戏原生命令（只处理 Dalamud 注册的插件命令）
- mountId 0 = 默认坐骑, mountId 1 = 陆行鸟（更明确）
- 跟随自动上坐骑 (`SprintController.TryMount`) 和飞旗按钮使用同一 mountId

---

### 11. 传送命令选择 (v1.6.0)

**方案**: `Telepo.Teleport(aetheryteId, 0)` — FFXIVClientStructs 原生调用

**原因**: `ProcessCommand("/tp id")` 可能因 Dalamud 版本差异无效。原生 Telepo 直接调用游戏内存函数，最底层最可靠。

---

### 12. 折叠按钮居中 (v1.6.0)

**方案**: 宽度 18px → 24px，移除自定义 `FramePadding`，与主按钮共用默认间距使高度一致。
`SameLine` 间距 2px → 19px (≈0.5cm)。

---

### 13. 目标丢失续行 (v1.7.0)

**方案**: 目标消失时保存 `_lastKnownTargetPos`，调用 `vnavmesh.MoveTo(最终位置)` 沿原方向续行，状态切 `TargetLost`。每帧检测：
- 目标重现 → `ResumeFollow()` 立即恢复
- 1.5s 超时 → 停移动，清目标

**分支**: 续行开关关闭时走原逻辑（直接停+PauseOnTargetLost）。

---

### 14. 换图自动恢复跟随 (v1.7.0)

**方案**: `_lastTerritory` 跟踪上次区域，帧回调 `CheckZoneChange` 检测 `TryGetTerritory` 变化。
- 区域不同 → 等 `BetweenAreas` 结束 → 延迟 1.5s → `FollowEngine.Start()`
- 只恢复曾经设过目标（`TargetName != null`）且不在 `Following` 态的情况

---

### 15. 远距目标暂停 2s 惯性移动 (v1.7.0)

**方案**: `_lastScanDistance` 跟踪每次扫描时的目标距离。仅在 `距离从 ≤100y → >100y`（跃迁）时触发：
- 存面朝方向 30y 点为移动目标，`vnavmesh.MoveTo` 沿该方向路径
- 置 `_farTargetPauseExpiry = now+2s`, `_farTargetMoveStopTime = now+1.5s`
- 每帧 `HandleFarTargetPause`：
  - 1.5s 内保持移动（vnavmesh 已在走）
  - 1.5~2s 停止移动，等 2s 到齐
  - 目标提前回到 100y 内 → 立即恢复
- 到期后清状态，走正常跟随逻辑继续追

**防重复**: `_lastScanDistance` 在触发时设为 >100y，下次扫描不会再触发，直到目标再次回到 ≤100y 后重新跃迁。

---

### 16. 自动接受传送邀请 (v1.7.0)

**方案**: 注入 `IGameGui`，帧回调 `CheckTeleportInvitation`：
- 守卫 1: `AutoAcceptTeleport` 开关
- 守卫 2: 仅 `Following` 态生效，防止误点其他确认框
- 用 `_gameGui.GetAddonByName("SelectYesNo")` 找确认框 addon
- 读 `AtkTextNode[2]` 文字，匹配 `"传送"` / `"Teleport"`
- `AtkUnitBase.FireCallback(1, &Int=0)` 触发"是"

**为什么用 IGameGui**: `GetAddonByName` 是获取 addon 指针的最简洁路径，比 AtkStage 遍历列表更清晰。

**为什么仅跟随态生效**: 防止其他插件弹出确认框（副本确认 / 退货确认等）被误接受。

---

### 17. 传送检测改进 (v1.7.0 — 后续完善)

**背景**: 早期传送检测仅匹配"传送" + "Teleport"，且仅限 `Following` 态。

**改进**:
- 加 `GetSelectYesnoAddon()` 双尝 `SelectYesno` 和 `SelectYesNo`（不同客户端大小写敏感不同）
- 文本读取优先级：`AddonSelectYesno.PromptText` 原生字段 → `GetTextNodeById(2)` → `TryReadSelectYesnoText()` 扫描所有文本节点
- 关键词扩展：加"瞬移"、"想要对你使用"
- 状态范围扩展：`Following` / `CatchingUp` / `Combat` / `TargetLost` 均接受
- 加 500ms 节流（`_lastTeleportClick`），避免同一弹窗每帧狂点
- 改用 `FireCallbackInt(0)` 代替手动构造 AtkValue（更稳定）

---

### 18. 副本通关暂停 (v1.7.0)

**需求**: 副本打完后自动停止跟随，防止出本后无意义乱跑。

**方案**: 注入 `IDutyState`（API 15 原生支持），监听 `DutyCompleted` 事件：
- 配置 `PauseOnDutyComplete`（默认 `true`，设置页"其他控制"中有开关）
- 事件触发时 `Pause("副本已完成")`，状态变 `Paused`
- 不影响正常跟随流程（出本后用户手动点继续即可）

**为什么用 IDutyState**: 比检测 `TerritoryType` 变化更准确（副本完成瞬间即触发，不依赖 `BetweenAreas`）。

---

### 19. 按钮颜色自定义 (v1.5.3)

**配置**: `BtnColorIdle` / `BtnColorFollowing` / `BtnColorPaused` 三个 `Vector4` 颜色属性。

**默认**: 灰(0.45,0.45,0.45) / 绿(0.20,0.80,0.30) / 琥珀(1.00,0.70,0.10)。

**实现**: 设置页有 `ColorEdit4` 控件 + 恢复默认颜色按钮，即时生效无需保存。

---

### 20. 自定义命令系统 (v1.5.x)

**背景**: 早期命令硬编码在 `Plugin` 中，扩展困难。

**方案**: `FollowConfig.CustomCommands` 列表 → `CustomCommandManager` 遍历注册。

**结构**:
- `CustomCommandEntry`：`Command`(命令文本) / `Action`(CommandAction枚举) / `Description` / `Enabled` / `ShowInHelp`
- `CommandAction` 枚举：循环控制(Pause/Resume/Toggle) / 跟随控制(Pause/Resume/Toggle/EmergencyStop) / 目标管理(FollowCurrent/Set/Clear) / 调试(ToggleDebug/OpenConfig/StatusReport)
- `Reload()`：先 UnregisterAll → 再遍历注册，支持运行时热加载

**默认命令**: `/flp` `/flr` `/flt` `/ff` `/fst` `/ft` `/fes` `/fdbg` `/ftar`

---

### 21. 两段式惯移完善 (v1.7.0)

**早期实现**: 目标丢失时仅沿面朝方向移动 1.5s。

**问题**: 目标传送后从 `ObjectTable` 消失，面朝方向未必是目标方向；面朝方向可能有墙。

**两段式方案**:
1. `ToLastKnown`：去 `_lastKnownTargetPos`（目标最后在表内时的坐标）
2. `FaceForward`：到达后沿面朝方向再移动 1.5s（总窗口 2s）

**关键参数**:
- `LastKnownArriveRange = 3y`：视为到达最后已知点
- `ToLastKnownTimeoutSeconds = 30s`：第一阶段超时，防止无限跑
- `FaceForwardDistance = 30y`：面朝方向移动距离
- `InertiaCancelRange = 100y`：目标重现且 ≤100y 时取消惯移

**防重复**: `_inertiaPhase` 枚举 + `ClearInertiaState()` 统一清理。

---

### 22. 零风险死代码清理 (v1.7.1)

**删除未用类型**: `StatusWindow`、`DistanceZone`、`FollowMode`（无引用）。

**移除 `getBossActive`**: Debug 窗曾注入恒 `false` 的回调且从未读取；Boss 判断仅在 `FollowEngine.IsBossTarget()`。

**对齐 `DebugLog`**: 属性默认 `Enabled = true`（原先类默认 false、Plugin 再 `{ Enabled = true }`，易误导）；同内容去重窗口与注释统一为 10 秒。

**其它**: 删除无人调用的 `SmartFollow()` 包装；`FormatHelper.Dist` 去掉 `>10` / `≤10` 相同返回分支。

---

### 23. 全局 Log 重构 (v1.8.0)

**背景**: 各模块各自持有 `IChatGui` 和 `IPluginLog` 引用，构造参数膨胀；聊天输出分散在 `_chatGui.Print()` / `PrintMsg()` 两套方式；`ChatOutput` 开关只在 `PrintMsg` 中生效，部分通知绕过了开关。

**方案**: 新增 `Utilities/Log.cs` 静态类，统一封装日志+聊天输出：
- `Log.Print(msg)` — 写入日志 + 按 `ChatOutput` 配置决定是否聊天输出
- `Log.Notice(msg)` — 写入日志 + 强制聊天输出（不受 `ChatOutput` 控制）
- `Log.Debug/Info/Warning/Error` — 仅写入日志（替代 `_logger.*`）
- 聊天消息使用 `XivChatType.Echo` + `AF` 图标（类似安米儿风格）
- `Plugin` 构造函数中调用 `Log.Initialize(_chatGui, _logger, () => _followConfig.ChatOutput)` 初始化

**影响范围**:
- 移除 `IChatGui` 引用：`FollowEngine`、`CustomCommandManager`、`SprintController`
- 移除 `IPluginLog` 引用：`FollowEngine`、`CustomCommandManager`、`CommandLoopController`、`IPCService`、`PluginStatusChecker`、`VnavmeshIPC`
- 所有 `_chatGui.Print()` → `Log.Notice()` 或 `Log.Print()`
- `FollowEngine.PrintMsg()` 方法删除，由 `Log.Print()` 替代
- `IPCService` 构造函数简化：不再需要 `IPluginLog`

**为什么用静态类**: 日志/聊天输出是横切关注点，几乎所有模块都需要。静态类避免了层层传递实例引用，减少构造参数数量。`Initialize` 一次后全局可用。

**为什么用 AF 图标 + Echo 类型**: 和安米儿保持一致，玩家可在聊天过滤中独立控制此插件消息。Echo 类型仅发送者可见，不影响其他人聊天框。

**删除未用类型**: `StatusWindow`、`DistanceZone`、`FollowMode`（无引用）。

**移除 `getBossActive`**: Debug 窗曾注入恒 `false` 的回调且从未读取；Boss 判断仅在 `FollowEngine.IsBossTarget()`。

**对齐 `DebugLog`**: 属性默认 `Enabled = true`（原先类默认 false、Plugin 再 `{ Enabled = true }`，易误导）；同内容去重窗口与注释统一为 10 秒。

**其它**: 删除无人调用的 `SmartFollow()` 包装；`FormatHelper.Dist` 去掉 `>10` / `≤10` 相同返回分支。

---

### 24. 副本暂停活跃过滤 + 扫描间隔调优 (v1.8.1)

**背景**: 副本通关事件触发暂停时，未区分引擎状态 —— 用户已手动暂停/空闲/紧急停止时也会被"副本已完成"打断并提示，体验突兀。

**方案**: `OnDutyCompleted` 增加状态过滤：

```csharp
if (_followEngine.State is not (FollowState.Following or FollowState.CatchingUp
    or FollowState.Combat or FollowState.TargetLost))
    return;
```

只在引擎**活跃**（跟随/追赶/战斗/目标丢失）时才暂停+提示；空闲/已暂停/紧急停止时直接忽略。

**为什么只列这四种**: 这是引擎实际执行跟随动作的所有状态。非活跃状态说明用户已主动停止或引擎无事可做，无需再触发暂停。

**扫描间隔 2s→1s**: `ScanInterval` 默认值从 2s 改为 1s（`FollowConfig.cs` + `DebugWindow` 恢复默认值同步），坐标/路径更新响应更快；≤5y 停走与脱战检测仍是每帧，不受影响。

---

## 注意事项 / 坑

### 编译环境 — ImGui.NET.dll 来源

API 15 的 dev 文件夹没有原生 `ImGui.NET.dll`，编译前需要手动复制：

```
来源: %AppData%\XIVLauncher\addon\Hooks\12.0.1.4-4-g0cbb4be2\ImGui.NET.dll
目标: %AppData%\XIVLauncher\addon\Hooks\dev\ImGui.NET.dll
```

**版本**: v1.82.0.0（来自 API 12 内置版本，与运行时签名兼容）

**不要**从 NuGet 缓存（`%USERPROFILE%\.nuget\packages\imgui.net\1.89.9.1\`）复制，版本不匹配会报 `MissingMethodException`。

**为什么自带**: API 15 运行时没有 `ImGui.NET.dll`，插件必须 `<Private>true</Private>` 自带。

### 使用 IGameGui 时注意

`IGameGui` 在 API 15 中可用（注入到 Plugin 构造函数），但**没有**读取旗标位置的方法（只有 `OpenMapWithMapLink` 用于设置旗标）。读取旗标请使用 `AgentMap`。

### 旗标坐标显示

旗标只有 X (East-West) 和 Z (North-South) 坐标，没有高度 (Y) 数据。MiniWindow 折叠区中显示 `X: {x:F1}  Z: {z:F1}`。

### 旗标区域检测

通过 `FlagMapMarker.TerritoryId` 判断旗标所在区域。跨区传送使用 `Telepo.Teleport(aetheryteId, 0)` 原生调用。`IAetheryteList` 提供已开通水晶列表。

### ProcessCommand 限制

`ICommandManager.ProcessCommand` **只处理 Dalamud 注册的插件命令**（如 `/vnav`）。游戏原生命令（`/mount`、`/tp`）不会被转发。必须走原生调用（`UseAction`、`Telepo.Teleport`）。

### 右键菜单颜色

MiniWindow 右键菜单颜色设计：
- **白色** (TextPrimary): 打开主窗口
- **琥珀色** (TextAccent): 状态报告 / 紧急停止 / 关闭窗口
- **默认**: 队伍成员列表（需 pop/re-push ImGui 样式栈围绕 party 部分）

### 配置项默认值

- `ScanInterval` = 2f (秒)
- `MoveThreshold` = 5f (yalms)
- 按钮颜色: BtnColorIdle (灰), BtnColorFollowing (绿), BtnColorPaused (琥珀)

---

## 开发前检查清单

1. ✅ 阅读本文了解项目上下文
2. ✅ `git log --oneline -10` 查看最近的提交
3. ✅ `git diff --stat` 查看当前未提交的改动
4. ✅ 确认 dev 目录有 `ImGui.NET.dll`（否则编译会失败）
5. ✅ `dotnet build -c Release` 验证零错误零警告

---

## 常见操作

### 构建 Release
```powershell
cd D:\gongju\FF14\开发\强效跟随
dotnet build -c Release
```

### 提交新版本
```powershell
git add .
git commit -m "v1.x.x: 描述"
git tag v1.x.x
```

### 复制到卫月插件目录
```powershell
$成品 = "bin\Release\net10.0-windows"
$卫月 = "$env:AppData\XIVLauncher\pluginConfigs\强效跟随"  # 视实际情况
Copy-Item "$成品\*" $卫月
```
